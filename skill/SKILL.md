# data preprocess skill

## rules

1. use python 3.11 style and do not add `__future__` imports.
2. after implementation, add tests under module `test` folder and run them.
3. after each completion, send a feishu message with what was completed.
